<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.musicapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BluesMusic"
            android:label="@string/blues_music"/>
        <activity android:name=".CountryMusic"
            android:label="@string/country_music"/>
        <activity android:name=".JazMusic"
            android:label="@string/jaz_music"/>
        <activity android:name=".PopMusic"
            android:label="@string/pop_music"/>
        <activity android:name=".RapMusic"
            android:label="@string/rap_music"/>
        <activity android:name="RockMusic"
            android:label="@string/rock_music"/>
        <activity android:name=".TestSound"
            android:label="Test"/>
        <activity android:name=".SearchBar"
            android:label="Search">
        </activity>
    </application>

</manifest>